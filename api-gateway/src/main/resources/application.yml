server:
  port: 8080

spring:
  application:
    name: gateway
  cloud:
    gateway:
      routes:
        - id: laliga-api
          uri: http://localhost:8081
          predicates:
            - Path=/laliga/**
    discovery:
      client:
        service-url:
          defaultZone: http://localhost:8761/eureka/
